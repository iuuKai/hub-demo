{
	"name": "@hub/blog",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"docs:dev": "vitepress dev --port 1000",
		"docs:build": "vitepress build",
		"docs:preview": "vitepress preview --port 1000",
		"sync": "node ../../scripts/sync-content.cjs --config blog",
		"dev": "pnpm sync && concurrently \"node ../../scripts/sync-content.cjs --watch --config blog\" \"vitepress dev --port 1000\"",
		"build": "pnpm sync && vitepress build",
		"preview": "vitepress preview dist --port 1000"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"devDependencies": {
		"concurrently": "^10.0.4",
		"vitepress": "^1.6.4",
		"vue": "^3.5.40"
	}
}
