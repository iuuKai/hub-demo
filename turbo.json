{
	"$schema": "https://turbo.build/schema.json",
	"globalEnv": ["SILICONFLOW_API_KEY", "SILICONFLOW_BASE_URL", "MODEL_NAME"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"env": ["SILICONFLOW_API_KEY", "SILICONFLOW_BASE_URL", "MODEL_NAME"],
			"outputs": ["**/dist/**"],
			"cache": true
		},
		"dev": {
			"persistent": true,
			"cache": false
		},
		"lint": {
			"cache": true
		}
	}
}
