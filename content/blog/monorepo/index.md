# 从多仓库到单仓库：我的 Vercel 多项目部署演进之路

## 前言

以往每写完一个 Demo，我都会新建独立 GitHub 仓库存放，采用「一仓库对应一个项目」的方式托管部署。随着 Demo 数量持续增多，零散仓库越来越难以维护；同时想要对外正常访问，每个项目都需要单独绑定自定义域名，整体维护成本与复杂度持续攀升。

我并不打算为这些演示项目购入多个域名，也不愿长期维护大量子域名。于是开始探索可行方案：能否依靠单一仓库统一管理所有子项目，仅使用一套自定义域名，实现自动部署并通过路径完成路由分发？
